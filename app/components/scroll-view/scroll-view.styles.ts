import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  refreshView: {
    overflow: 'hidden',
    backgroundColor: 'red',
    position: 'absolute',
    top: 0,
    left: 0,
    right: 0,
    alignItems: 'center',
    justifyContent: 'center',
  },
});
